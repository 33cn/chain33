version: '3'

services:
  chain33:
    build:
      context: .

  chain32:
    build:
      context: .

  chain31:
    build:
      context: .

  relayd:
    build:
      context: .
      dockerfile: Dockerfile-relayd

  btcd:
    image: suyanlong/btcd-run:latest
